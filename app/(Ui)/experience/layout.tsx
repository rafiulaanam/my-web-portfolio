import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Experience | Anam - Full Stack Developer",
};

export default function ExperienceLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
